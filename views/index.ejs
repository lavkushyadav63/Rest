<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>About Post</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Qura Post</h1>
    <% for (post of posts){ %>
        <div class="post">
            <h2 class="user">@<%= post.username %></h2>
            <h3 class="content">@<%= post.content %></h3>
            
            <br/>
            <a href="http://localhost:8080/posts/<%= post.id %>"> See in Details</a>
            <a href="http://localhost:8080/posts/<%= post.id %>/Edit">    Edit</a>
            <form  method="post"action="/posts/<%= post.id %>?_method=DETETE">
                <button>DETETE POST</button>
            </form>
           

        </div>


     <% } %> 
     <br>
     <br>
     <a href="http://localhost:8080/posts/new"> create  a new post</a>
    
</body>
</html>