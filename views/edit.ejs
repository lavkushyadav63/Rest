<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Your Form </title>
</head>
<body>
    <h2>Edit Your Form</h2>
    <p> Post id : <%= post.id %></p>
    <p> Post user : <%= post.userame %></p>
    <form  method="post"action="/posts/<%= post.id %>?_method=PATCH">
        <textarea  rows="11" cols="44" name="content"><%= post.content %></textarea>
        <button>Submit</button>
    </form>
</body>
</html>